<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.microsoft.cognitiveservices.speech.samples.InterpretAR">
    <!-- Request permission to use camera and adds that feature -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_action_name"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_action_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="MeltdownLabs.microsoft.cognitiveservices.speech.samples.InterpretAR.AboutUsActivity"></activity>
        <activity android:name="MeltdownLabs.microsoft.cognitiveservices.speech.samples.InterpretAR.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="MeltdownLabs.microsoft.cognitiveservices.speech.samples.InterpretAR.TranslatarActivity"
            android:configChanges="orientation|screenSize"
            android:exported="true"
            android:label="@string/app_name"
            android:parentActivityName="MeltdownLabs.microsoft.cognitiveservices.speech.samples.InterpretAR.MainActivity"
            android:screenOrientation="landscape"
            android:theme="@style/generalnotitle">

            <!-- configure this activity to use landscape orientation -->
        </activity>
    </application>

</manifest>